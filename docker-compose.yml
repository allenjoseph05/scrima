services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: scrima
      POSTGRES_PASSWORD: scrima
      POSTGRES_DB: scrima
    ports:
      - '5433:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
